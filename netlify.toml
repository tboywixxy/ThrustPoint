[build.environment]
  # These values are public service/contact configuration, not credentials.
  # Keep credential keys such as ZOHO_SMTP_PASSWORD covered by secret scanning.
  SECRETS_SCAN_OMIT_KEYS = "ZOHO_SMTP_HOST,ZOHO_SMTP_USER,CONTACT_RECEIVER_EMAIL"
